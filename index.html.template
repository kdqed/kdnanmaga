<html>
<head>
  <title>kdnanmaga / Karthik D</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="theme-color" content="#12110d">
  <meta charset="utf-8">
  <meta name="description" content="Writes code, blogs and stories">
  
  <meta property="og:title" content="kdnanmaga / Karthik D" />
  <meta property="og:url" content="https://kdnanmaga.xyz" />
  <meta property="og:description" content="Writes code, blogs and stories">
  <meta property="og:image" content="/assets/dp.png">
  <meta property="og:type" content="website" />
  
  <meta name="twitter:card" content="app"></meta>
  <meta property="twitter:image" content="/assets/cov.png">
  
  
  <link rel="shortcut icon" type="image/png" href="/assets/dp.png"/>
  <style>
    :root{
      --l1: #f9f9f9;
      --l2: #d6d6d6;
      --theme: #5d8550;
      --d1: #12110d;
      --d2: #191812;
      --d3: #484633;
      --blue-l: #8bdefe;
      --blue-d: #6bafca;
    }
    * {
      box-sizing: border-box;
    }
    @font-face {
      font-family: font1;
      src: url(/assets/jost.otf);
    }
    body {
      font-family: font1;
      padding: 0px;
      margin: 0px;
      font-size: 1rem;
      background: var(--blue-l);
      background: url(/assets/cov.png);
      background-position: center center;
      background-size: cover;
      color: var(--d3);
      text-align: center;
      min-height: 100vh;
      display: grid;
      grid-template-rows: 128px 1fr;
      justify-items: center;
    }
    h1,h2,h3,p{
      font-weight: 200;
    }
    h1::before{content: "@";}
    h1{font-size: 133%;}
    h2{font-size: 115%;}
    main{
      display: inline-block;
      width: 100%;
      max-width: 640px;
      text-align: center;
      color: #fff;
      background: #12110dc0;
      padding: 8px;
      border-radius: 16px 16px 0 0;
    }
    h2{
      color: var(--blue-l);
      margin-top: 32px;
    }
    ul{
      list-style: none;
      margin-left: 0;
      padding-left: 0;
    }
    li {
      display: inline-block;
      padding: 4px 16px;
      border: 1px solid var(--blue-l);
      border-radius: 100px;
      color: var(--blue-l);
      margin: 8px;
      
    }
    a{
      color: inherit;
      text-decoration: none;
    }
  </style>
</head>
<body>
<div></div>
<main>
{{htmlgoeshere}}
</main>
</body>
</html>
